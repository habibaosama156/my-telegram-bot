export PATH=$PATH:/data/data/com.termux/files/usr/bin/heroku/binCexport PATH=$PATH:/data/data/com.termux/files/usr/bin/heroku/binexport PATH=$PATH:/data/data/com.termux/files/usr/bin/heroku/binexport NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
